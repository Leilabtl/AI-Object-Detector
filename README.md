# Attempt-2
Machine Learning Home Assignment 
# 🧠 Teachable Machine Image Classifier

This repository contains a custom **Image Classification** model built using [Google's Teachable Machine](https://teachablemachine.withgoogle.com/). It uses **TensorFlow.js** to perform real-time classification directly in your web browser.

## 🚀 How to Run This Project

### 1. Project Structure
To make sure the code works, organize your files like this:
```text
├── index.html          <-- Paste your HTML/JS code here
└── my_model/           <-- Exported model folder
    ├── model.json
    ├── metadata.json
    └── weights.bin
